---
timestamp: 2026-08-28T150000Z
title: PR 4: reviewed Docling Serve package
branch: feature/docling-serve-extractor-package
status: complete
---

# PR 4: reviewed Docling Serve package

## Progress

Issue #1159 PR 4, branch `feature/docling-serve-extractor-package`
(parent #1166, exact head `250b3a4d`).

- Reviewed `ExtractorPackages/DoclingServe` package: manifest revision 2,
  protocol revision 2, one PDF registration declaring the optional
  `api-token` secret requirement. Source of truth:
  `tools/docling-serve/docling_serve_extractor.py` (stdlib Python); the
  entry point is generated by `scripts/sync-extractor-packages.sh`, which
  also records the source digest in `ExtractorPackages/sources.lock.json`.
  The package posts multipart to `<endpoint>/v1/convert/file`, sends the
  token ONLY in `X-Api-Key`, writes Markdown to the requested output path,
  and emits bounded frames — never printing headers, credential input, or
  token-bearing errors.
- Compiled reviewed identity
  `org.selfdrivingwiki.docling-serve 1.0.0` digest `e072e3bd…`; validated by
  the real validator in `ReviewedExtractorPackageTests` with the digest
  pinned byte-for-byte.
- Routing: `ProcessExtractionServices.reviewedDoclingLogical`; the legacy
  `.doclingServe` selection maps to the reviewed lineage with NO silent
  fallback; `preparePDF` pairs the Docling lineage with the `.doclingServe`
  backend tag (never the interim `.localPdf2md` tag); the route host catalog
  presents Docling as a reviewed package choice.
- Host bypass removal: the built-in Docling registry entry, the
  `.doclingServe` case in `ExtractionRuntimeFactory`, and
  `DoclingExtractionPlugin` are gone; `DoclingServeClient` remains only
  behind the Settings connection test. Enforced by
  `DoclingExecutionScopeAuditTests` source contracts.
- Public settings: typed `doclingServeTimeoutMilliseconds` on
  `ExtractionConfig` (600 s compatibility default, deterministic Codable),
  a Settings timeout field, and a per-lineage operation-configuration
  provider supplying endpoint + timeout to the package through the public
  envelope.
- The token keeps its existing Keychain item
  (`extraction.docling-serve-token`) and reaches the package only after
  explicit authorization (Settings → Package Credentials).

## Verification

reviewed-package validation + digest pin, legacy mapping trio
  (lineage, no `.localPdf2md` tag, catalog category), execution-scope audit,
  timeout round-trip/compatibility. Gates: make extractor-packages --check,
  make lint, make build, make test, git diff --check.
- Docs: `plans/credential-service.md` (new, indexed from PLAN.md),
  `plans/dynamic-extractor-packages.md` revision-2 section, user guide, and
  the VoiceOver smoke script.
