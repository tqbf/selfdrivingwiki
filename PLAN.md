# Self Driving Wiki

**What this is.** A native macOS SwiftUI wiki backed by SQLite. An optional
**File Provider extension** mirrors content read-only onto the filesystem under
`~/Library/CloudStorage/Self Driving Wiki-<wiki name>` so Unix tools and agents
can browse it (`find`, `cat`, `grep`). When the extension is not available (e.g.
unsigned dev builds outside `/Applications`), the app reads everything from
SQLite directly. You edit in the app; the mount reflects every change when
enabled. It ingests dropped files, stores them as verbatim BLOBs, and projects a
singleton agent system prompt as `CLAUDE.md` + `AGENTS.md` at the root. Runs
locally only — free, local dev signing; no Developer ID / notarization.

**The File Provider extension is now optional.** The in-app experience —
displaying pages, browsing ingested files, and running agent operations — reads
from SQLite via `wikictl` (pages, raw files) and staged scratch directories
(sources, wiki-state). The extension remains in the tree as an opt-in mount for
Terminal and Finder browsing when signing is available, but it is no longer
load-bearing for the app to function.

**Where to find things.**

- **This file (`PLAN.md`)** — the master index: the doc map below, milestone
  status, and the build quick-reference.
- **`PROGRESS.md`** — the running log, newest first: what was built each step and
  the evidence each gate passed. *To get a future agent up to speed, read
  `PLAN.md` then `PROGRESS.md`.*
- **`plans/`** — the deep design docs (architecture, build, File Provider,
  signing); see the table below for which is which.
- **`ISSUES.md`** — known limitations we've chosen to live with.
- **`SWIFTUI-RULES.md`** / **`CLAUDE.md`** — coding rules and the working
  agreement (docs to keep, skills to use, PR rules).

## Documentation index

| Doc | What it covers |
| --- | --- |
| [`docs/user-guide/`](docs/user-guide/README.md) | **User guide.** What the user sees and does — not architecture. Covers concepts, getting started, the interface, pages & links, sources & ingestion, chatting with the agent, bookmarks/search/navigation, multiple wikis, settings, the queue, and keyboard shortcuts. Start here for user-facing documentation. |
| [`README.md`](README.md) | **Start here (new developers).** What Self Driving Wiki is, the non-negotiable read-only-mount / write-via-`wikictl` invariant, quick start (`make` targets + the runtime gotchas), repo layout, and a tour of how it works. |
| [`plans/architecture.md`](plans/architecture.md) | **The system map.** Components/targets, the per-wiki SQLite data model + migration ladder + `changeToken()`, the File Provider projection, the read/write split + change bridge, the `claude -p` operations (Ingest tiering, Query, Lint), URL ingest, and the key invariants/gotchas. Read after the README to go deep. |
| [`plans/INITIAL.md`](plans/INITIAL.md) | Original full product/architecture plan (milestones, schema, File Provider design, definition of done). Source of truth for *what we're building*. |
| [`plans/llm-wiki.md`](plans/llm-wiki.md) | **Next major effort:** turning Self Driving Wiki into a self-maintaining LLM Wiki — **many** wikis (one SQLite DB + one File Provider domain each), with `claude -p` authoring/maintaining each one by writing via a new `wikictl` CLI (read via the mount, write via the CLI). Locked decisions, components, and the Phase 0 → A–D plan. Read before Phase 0. |
| [`plans/page-reader-ui.md`](plans/page-reader-ui.md) | **Current UI direction:** page detail is reader-first because the agent should maintain wiki content; manual source editing is an explicit, rare mode. |
| [`plans/page-body-contract.md`](plans/page-body-contract.md) | **Page body contract:** `body_markdown` in SQLite stores clean body only (no H1, no frontmatter); the file provider generates both on the fly via `PageMarkdownFormat`. Covers the outline-flicker fix, migration strategy, frontmatter schema, and editor warning. |
| [`plans/query-conversation.md`](plans/query-conversation.md) | **Current Query direction:** a dedicated sidebar page with an interactive Claude session; output-first chat by default, hidden tool/internal rows behind a checkbox, and writes via `wikictl` only when the user asks to persist changes. |
| [`plans/BRINGUP.md`](plans/BRINGUP.md) | The 4-phase bring-up plan from skeleton to v0 (groups INITIAL.md's M0–M6). Source of truth for *the order we build in*. |
| [`plans/build-environment.md`](plans/build-environment.md) | How the app is built: SwiftPM + `build.sh` + `Makefile`, signing, icon generation, app-bundle layout. Source of truth for *how we build and run*. |
| [`plans/bundle-uv.md`](plans/bundle-uv.md) | Bundle the `uv` runtime (single static binary, like `bun`) into `Contents/Helpers` so the `pdf2md` PEP 723 script is self-contained — no system uv needed. (#766) |
| [`plans/file-provider.md`](plans/file-provider.md) | File Provider extension build + the 5 hard-won gotchas (entry-point recursion, entitlements⊆profile, user-enable toggle, /Applications, keychain). Proven by the 2026-06-15 spike. Read before Phase 2. |
| [`plans/signing.md`](plans/signing.md) | The Apple cert / App Group / File Provider provisioning checklist (manual portal). Do this before Phase 2. Source of truth for *the Apple incantations*. |
| [`plans/zotero-integration.md`](plans/zotero-integration.md) | browse a Zotero library from inside the app, ingest PDF/Markdown attachments through the existing ingest pipeline. |
| [`plans/zotero-source-link.md`](plans/zotero-source-link.md) | Stamp Zotero item key+title on ingested files (v8→v9 migration) and show a "View in Zotero" link in the ingested-file detail view. |
| [`plans/pdf-extraction.md`](plans/pdf-extraction.md) | Add docling + granite-docling pipeline. A `pdf2md` CLI converts PDFs to markdown at ingest time; extracted markdown stored as a sibling `ingested_files` row, projected on the mount. Agent prefers `.md` siblings, falls back to `Read` on the original. |
| [`plans/pdf-extraction-backends.md`](plans/pdf-extraction-backends.md) | **Pluggable extraction backends.** A `MarkdownExtractor` protocol + `ExtractionCoordinator` so PDF→Markdown can run via **local pdf2md** (default), **Claude** (Anthropic API), **Gemini** (Google AI), or a self-hosted **Docling Serve**. Backend-agnostic storage (no schema change), a Settings tab that shows only the selected backend's config + per-backend Test Connection, and config/Keychain/HTTP patterns mirroring Zotero. |
| [`plans/extraction-framework.md`](plans/extraction-framework.md) | **General extraction framework (#799):** 4-PR staged plan to bring HTML + Podcast extraction to parity with PDF — no auto-extraction at ingest, user chooses backend and triggers extract. PR1 (merged #802) shipped enums + Settings pickers (scaffolding); PR2 added the HTML Extract button + Re-extract menu (inline path, not the PDF-coupled queue); PR3 removes HTML auto-extraction (the three `enrichWithDefuddle` callers); PR4 is the podcast framework. See `plans/extraction-framework-pr2.md` for PR2's deep design. |
| [`plans/semantic-search.md`](plans/semantic-search.md) | Semantic (meaning-based) search over pages using sqlite-vec + Apple NLEmbedding. Embedding at save time, cosine-similarity ranking inside SQLite, search bar in the sidebar, `wikictl search` for the agent. v7 migration. |
| [`plans/source-semantic-search.md`](plans/source-semantic-search.md) | Semantic search over **sources** — mirrors the page pipeline (sqlite-vec cosine, `EmbeddingService`) on a new `source_embeddings` table (v11→v12). Re-embed hooks on `appendProcessedMarkdown`/`renameSource`, a Sources sidebar search box, and `wikictl source search` for the agent. Embeds processed-markdown body + filename; name-only fallback for un-extracted/binary sources. **Caveat (discovered):** the vec layer never loads on macOS (system SQLite = `OMIT_LOAD_EXTENSION`); see `search-fts5-hybrid.md`. |
| [`plans/search-fts5-hybrid.md`](plans/search-fts5-hybrid.md) | **Done.** Fixes search end-to-end: (1) FTS5/BM25 backbone over the full body (always-on, testable — replaces the filename-only `LIKE` fallback), (2) fix the broken vec layer via a static `sqlite-vec.c` amalgamation (`-DSQLITE_CORE`, direct `sqlite3_vec_init`), (3) RRF hybrid reranker fusing BM25 + cosine. Pages use external-content FTS5 over `pages` (triggers, zero Swift change); sources use a `source_search` sidecar (versioned body). v13 migration. |
| [`plans/mlx-minilm-design.md`](plans/mlx-minilm-design.md) | **Design (not yet implemented).** Replace `NLEmbedding` (512-dim) with MLX `all-MiniLM-L6-v2` (384-dim) on Metal/GPU behind a new `Embedder` protocol — 100–1000× faster, safe off-main (no UI jank), no `std::bad_alloc` cliff. Uses `mlx-community/all-MiniLM-L6-v2-bf16` + Apple's `MLXEmbedders` (no conversion pipeline), an `embedding_meta`-driven dimension cutover wipe, and a phased plan (Phases 0–3). Pivoted from the earlier CoreML/ANE design. |
| [`plans/markdown-folder-import.md`](plans/markdown-folder-import.md) | Import an entire folder of Markdown files (Obsidian vault, LogSeq graph, or any `.md` directory) as source material. Recursive walk, .md filter, filename dedup; lands files in `ingested_files` for the agent to curate via Ingest. |
| [`plans/sources-redesign.md`](plans/sources-redesign.md) | **Design phase:** rebrand "ingested files" as first-class **sources** — `[[source:...]]` wikilinks, display-name editing, processed-markdown projection, full rename + v10 migration. |
| [`plans/fix-phase-a-source-bugs.md`](plans/fix-phase-a-source-bugs.md) | **Blocks Phase B.** Fixes two bugs shipped in Phase A (PR #31): `source_links` FK missing `ON DELETE CASCADE` (v11 rebuild migration) and the File Provider still projecting `files.jsonl`/`files/` instead of `sources.jsonl`/`sources/`. |
| [`plans/fileprovider-schema-migration-and-cache-warming.md`](plans/fileprovider-schema-migration-and-cache-warming.md) | **Infrastructure for source share.** Schema version migration (auto-tears-down domains on container renames like `files`→`sources`), top-down cache warming so the daemon enumerates `sources/by-name/` before the share sheet needs it, shared `source-by-name:` identifier prefix across app and extension. |
| [`plans/share-pages-and-sources.md`](plans/share-pages-and-sources.md) | **Share pages and sources.** Share button on every page/source surface (detail toolbar + sidebar context menu). Daemon-resolved URLs via `getUserVisibleURL` — no path construction. Batch share resolves all selected item URLs in parallel via `withTaskGroup`. Single and multi-select for both pages and sources. |
| [`plans/phase-b-source-wikilinks.md`](plans/phase-b-source-wikilinks.md) | **Phase B, re-grounded.** Implements `[[source:...]]` wiki links correctly, superseding the Phase B portion of `sources-redesign.md` wherever they conflict. Render source links as `wiki://source?title=` (mirror pages), shared normalizer + case-insensitive resolution, single-transaction `replaceLinks` over both link tables, unified `links.jsonl` with a `type` field. Depends on `fix-phase-a-source-bugs.md`. |
| [`plans/content-type-over-extension.md`](plans/content-type-over-extension.md) | **Cross-cutting prerequisite.** Make the content-derived `mime_type` the behavioral authority: extract a `ContentSniff` helper, make `addSource` sniff bytes (not trust the extension), MIME-first `WikiFSItem.contentType`, Zotero filter, and a grep guard against new extension checks. Phase C depends on it. |
| [`plans/phase-c-source-markdown-projection.md`](plans/phase-c-source-markdown-projection.md) | **Phase C, re-grounded.** Project a `.md` sibling (HEAD of `source_markdown_versions`) for sources with a conversion chain. Fixes the parent design's two false assumptions (the change bridge doesn't see chain edits; markdown sources don't get chains) and encodes the git-lite model: PDF-only chains, HEAD-only projection, revert/compare-ready. Depends on `content-type-over-extension.md`. |
| [`plans/markdown-anchors.md`](plans/markdown-anchors.md) | **New feature.** In-document anchors and footnote citations: `[[Page#Section]]` (heading slug) for pages, `[[source:Name#"quoted passage"]]` (text-quote) for sources — quote-based so citations survive PDF re-extraction (nothing stored in the source text). Render-time block ids + navigate-then-scroll via a pending anchor. Footnotes already linkify, so they get it free. Includes agent-prompt docs for footnote + cite-by-quote conventions. |
| [`plans/page-embed-v2.md`](plans/page-embed-v2.md) | **Plan v2: generalized `![[X]]` embed** (pages + non-media sources). A `![[PageName]]` or `![[source:notes.txt]]` now renders a collapsed `<details>` disclosure, lazily fetched + rendered on expand through the same `ReaderMarkdown.prepared` + `MarkdownHTMLRenderer.render` + `evaluateJavaScript` seam the reader uses. Media sources (image/video/audio/PDF/Mermaid) stay inline unchanged. Render-only — the `WikiLinkParser.parse` L184 reject gate stays; cycle safety via lazy-collapse (primary) + a JS-side visited-set (defense-in-depth). |
| [`plans/phase-d-display-names-rename.md`](plans/phase-d-display-names-rename.md) | **Phase D, re-grounded.** Editable source display names + rename propagation. Supersedes the parent's Phase D (fragment-blind, body-scan): rewrite swaps the link base only — preserving `#fragment` and `\|alias` (markdown-anchors) — driven off the `source_links` graph (new read helper), code-span-safe, transactional, with by-name projection switched to `display_name` and the change bridge refreshed on rename. Drops the false "general capability" claim (page-rename rewriting is a follow-up). |
| [`plans/extraction-vs-ingestion-lock.md`](plans/extraction-vs-ingestion-lock.md) | **Current direction:** treat markdown *extraction* (pdf2md) and agent *ingestion* (the `claude -p` run) as two phases. Extraction must never lock queries, edits, or another file's ingest. Split the overloaded `ingestingFileIDs` into `extractingFileIDs` (pdf2md phase) + `ingestingFileIDs` (agent-spawn phase), plus an optional separate extraction lock to serialize pdf2md without touching the spawn slot. |
| [`plans/wikictl-file-reads.md`](plans/wikictl-file-reads.md) | A `wikictl file` command family (`list` / `cat` / `export`) so the agent reads raw ingested files from SQLite instead of the File Provider mount during Query. First concrete step of the provider-decouple effort; rewrites the Query prompt off `$WIKI_ROOT/files/...`. |
| [`plans/tab-context-menu-rebuild.md`](plans/tab-context-menu-rebuild.md) | Multi-tab editor space — design of record. `activeTabID: UUID?` source of truth (no index arithmetic), one-intent-per-method tab ops, tab reuse, native SwiftUI `.contextMenu` (Close / Close Others / Close Tabs After / Close All), opacity-fade close button, responsive shrink-to-fit strip with overflow menu. 43 `EditorTabTests` + 7 `TabBarLayoutTests`. |
| [`plans/link-context-menus.md`](plans/link-context-menus.md) | **Implemented (`feature/link-context-menus`).** Right-click link context menus (Suggest for missing links, Find Similar for any link, Copy as wiki-link, Open in Browser + Copy Link) — right-click now selects the **whole link**, not the word under the cursor. Documents the Textual blocker (its `NSTextInteractionView` owns right-click + the menu internally; `model.url(for:)` hit-test is internal) and the decision to vendor Textual in-repo. Copy File Path + Edit Link are classified but deferred (see `PROGRESS.md`). **Issue #188 add-on:** an **Add Bookmark…** item (`.addBookmark` action) now appears on **resolved internal wiki links** (`[[Page]]` / `[[source:Name]]`) — resolves the target id and files it into a folder via `BookmarkTargetPickerSheet` (new `\.addBookmarkHandler` env value; no fetch). |
| [`plans/reader-editor-zoom.md`](plans/reader-editor-zoom.md) | Safari-style text zoom for the page reader and monospace editors. Pure `ZoomScale` model (0.5–3.0, ×/÷1.1 step, scroll-step accumulation); `@AppStorage` keys `reader.zoom` and `editor.zoom` (first in the app); ⌘+/⌘=/⌘−/⌘0 via `.opacity(0)` buttons (`ZoomShortcuts`) and ⌘+scroll (`ZoomScroll`) in `PageDetailView` and `SourceDetailView`. No menu item. 21 `ZoomScaleTests`. |
| [`plans/dirty-editor-protection.md`](plans/dirty-editor-protection.md) | **Implemented (`feature/dirty-editor-protection`).** Three editor-UX fixes: outline toggle button added to the edit-mode toolbar in both `PageDetailView` and `SourceDetailView`; per-tab edit-mode persistence via `EditorTab.isEditing` (switching tabs and back restores edit mode); close-tab confirmation (`pendingCloseTabID` deferred close + "Close Tab?" alert) so ⌘W / × while editing cannot silently discard the edit session. Sources flush `editBuffer` before `confirmCloseTab()` to ensure `file.id` is still the closing tab's source. |
| [`plans/drag-wikilinks.md`](plans/drag-wikilinks.md) | **Implemented (`feature/drag-wikilinks`, PR #623 / issue #616).** Sidebar→editor drag-drop. Dragging a Page/Source/Chat (or a Bookmark folder) from the sidebar into the page editor or source markdown editor in edit mode inserts a canonical ULID-pinned wikilink (`[[page:ULID\|alias]]` / `[[source:ULID\|alias]]` / `[[chat:ULID\|alias]]`) at the visual drop point. A folder (or multi-row selection) produces a flat depth-0 markdown list (`- [[kind:ULID\|name]]` per line). Pure `DroppedLinkFormatter` (Foundation-only; ULID-canonical → rewriter idempotency fast-path on save). `DropLinkTextView: NSTextView` registers `wikiSidebarItem` ALONGSIDE the inherited text types (the load-bearing divergence from `WikiReaderView` — #133/#385 doesn't apply on a terminal NSTextView). `SidebarDropBuilder` resolves display names via `store.resolveAttachmentName` and guards on `store.agentRunCount > 0` (Step 4 protection: never silently insert into a buffer an agent is about to overwrite). v1 ships Option B (flat list for folders); nested indentation is a documented follow-up — the `markdownList(for:)` signature is already depth-aware. |
| [`plans/quote-highlight-and-scroll.md`](plans/quote-highlight-and-scroll.md) | **Implemented (`main`).** Extends markdown-anchors' navigate-then-scroll with **highlight the exact quoted passage** and **work on PDF sources**. Markdown: `WikiLinkStylingParser.highlightQuote` + pure `quoteRange` whitespace-tolerant first-match → `.backgroundColor` (`NSColor.findHighlightColor`). PDF: `PDFKit.findString` → `currentSelection` → `scrollSelectionToVisible`. `pendingScrollAnchorVersion` counter for re-click reactivity. 17 new `QuoteHighlightTests` + 2 `AnchorBlockTests` regression; 911 total. |
| [`plans/source-web-reader.md`](plans/source-web-reader.md) | **Implemented (`feat/source-web-reader`, PR #50).** WKWebView reader for large sources (500 KB+), where the native Textual reader freezes on whole-document layout (preprocess + parse is only ~260 ms on 513 KB — the freeze is layout). Size-gated (default 96 KB; native reader stays for small docs / pages), async load, shared `WikiFootnoteMarkdown` / `WikiLinkMarkdown` pre-pass, swift-markdown HTML render, external `#section` anchors + `[[source:Name#"quote"]]` highlight, app-matched theming. Sources don't reference each other via wiki links, so ghost-link coloring is N/A. ~130–280 ms appear-to-painted vs ~10 s native. |
| [`plans/agent-command-settings.md`](plans/agent-command-settings.md) | **Removes the hardcoded `claude -p`.** A new Settings → Agent tab (structured fields, not a free-form string) lets the user set the executable (binary or wrapper script, e.g. `sandbox-exec`), prefix arguments, a model override, and extra environment variables. The app keeps owning the load-bearing flags (`-p`, `--output-format stream-json …`, `--append-system-prompt`, `--dangerously-skip-permissions`, `--agents`). New `AgentCommandConfig` mirrors `ZoteroConfig`; default reproduces today's run exactly. |
| [`plans/url-context-menu-add.md`](plans/url-context-menu-add.md) | **Implemented (`feat/url-context-menu-add`, PR #52).** Right-click an external http(s) link → **Add as Source** leads the context menu, opening the "Add from URL" sheet pre-filled (same Fetch + ingest as the toolbar button). New pure `.addAsSource` action (http(s) only; `mailto:` etc. excluded since `URLIngestService` rejects them); the sheet reaches the deep reader via a single `\.addURLHandler` environment value (no per-view wiring), and `AddFromURLSheet(store:initialURL:)` pre-fills the field. **Both readers:** the native Textual reader (`MarkdownPreview`) and the WKWebView large-source reader (`SourceWebView` — a `SourceDetailWebView` subclass overrides `willOpenMenu` since WebKit has no macOS context-menu API; DOM hit-test filters http(s)). |
| [`plans/editor-autocomplete.md`](plans/editor-autocomplete.md) | **Implemented (`editor-autocomplete`, issue #680).** Wiki-link autocomplete in the page/source markdown editor — reuses the chat composer's `ChatAutocompletePanel` (generalized to `present(caretRect:in:placement:)` in #684) and the same Tantivy fuzzy `autocomplete(partial:kinds:distance:2,limit:8)` fetch path, with `.below` placement instead of `.above`. The autocomplete state machine is extracted from `ComposerTextView.Coordinator` into a reusable `WikiLinkAutocompleteController` (`Sources/WikiFS/Editor/WikiLinkAutocompleteController.swift`) so the chat composer and the editor share one implementation; `ScrollableTextEditor` adds `autocomplete`/`autocompletePlacement`/`autocompleteDebounce`/`autocompleteScheduleDebounce` parameters and a `dismantleNSView` teardown path. `SidebarDropBuilder.wikiLinkAutocompleteHooks(store:)` is the hooks factory (mirrors `ChatView.chatAutocompleteHooks`). Same canonical `[[kind:ULID\|Title]]` insertion form as drag-wikilinks (#616). 12 new `EditorAutocompleteHostedTests`; chat composer tests unchanged. |
| [`plans/quota-fallback.md`](plans/quota-fallback.md) | **Issue #727 — Provider quota-exhaustion detection + fallback.** When an agent provider returns a quota-exhaustion error, mark it dead until a reset time and fall through to the next enabled provider in the ingestion stage's chain. Passive, first-fail detection operating solely on the ACP SDK's `JSONRPCError` (`.code`/`.message`/`.data` — the transport is stdio JSON-RPC, no HTTP). Claude text heuristic ("session limit"/"weekly limit"/"Opus limit"); z.ai numeric code whitelist (1310/1316/1317/1318/1319/1308), excluding transient codes (1302/1305/1313). Per-stage fallback chains via `providerChain(forStage:)` + `QuotaFallbackCoordinator` (dead-provider map, @MainActor). `runPhase` returns a typed `PhaseOutcome` so the launcher's `runPhaseWithFallback` can walk the chain. Cross-provider fork reconciliation: `forkFrom=nil` when the executor's provider differs from the planner's actual post-fallback provider. Ingestion-only (chat/extraction deferred). |
| [`SWIFTUI-RULES.md`](SWIFTUI-RULES.md) | Hard-won SwiftUI/macOS rules. Apply when writing or reviewing any view. |
| [`CLAUDE.md`](CLAUDE.md) | Working agreement (docs, skills to use, PR rules). |
| [`ISSUES.md`](ISSUES.md) | Known limitations we've chosen to live with (with context to revisit), e.g. the ~5s replicated-File-Provider read-after-write window. |
| [`plans/textual-to-wkwebview.md`](plans/textual-to-wkwebview.md) | **Implemented (`feature/textual-to-wkwebview`).** Replaced the vendored Textual reader with the WKWebView + `MarkdownHTMLRenderer` path across **every** reader surface (pages, sources, system prompt, changelog, agent transcript), then deleted `Packages/Textual/` entirely. Generalized `SourceWebView` → `WikiReaderView`, filled the two real gaps (ghost-link coloring via CSS, ⌘+ zoom via `WKWebView.pageZoom`) and fixed the `wiki://` routing bug (query-based `WikiLinkMarkdown` helpers). The agent transcript gained `[[wiki-link]]` support (linkify pre-pass + `onWikiLink` routing closure). **Mermaid diagrams:** ` ```mermaid ` fenced blocks render as inline SVG (no network). Vendored **Mermaid 10.9.6** UMD build at `Resources/mermaid.min.js` (copied into the bundle as `mermaid.js` by `build.sh`); the library + bootstrap are inlined only on pages that contain a mermaid block. Re-vendor: re-download `https://cdn.jsdelivr.net/npm/mermaid@10.9.6/dist/mermaid.min.js` (verify it starts with the UMD wrapper `(function(...){typeof exports=="object"...`); **do not bump to v11** (its ESM build exposes no clean `window.mermaid` global). SHA256 `eda3a0ad572bbe69a318c1be0163e8233dd824f3f12939e5168feba207767151`. |
| [`plans/sandbox-agent.md`](plans/sandbox-agent.md) | **Agent seatbelt sandbox.** Confines the spawned agent's filesystem WRITES to a strict allowlist (per-run scratch dir + active wiki DB) via macOS's seatbelt (`sandbox-exec`). Provider-agnostic, macOS 15+, opt-in. Reads/network/exec stay open; the provider's config/temp are relocated into the scratch dir. Default-deny writes → no backdoors, no credential tampering, cross-wiki DB confinement. |
| [`plans/markdown-linter.md`](plans/markdown-linter.md) | **Markdown linter (cosmetic auto-fix on save).** Vendored markdownlint 0.41.0 + helpers 0.31.0, bundled to a single IIFE and run in a `JavaScriptCore` `JSContext` (no Node at runtime). Curated cosmetic-only rule set (whitespace, blank-line spacing, trailing newline); every enabled rule is auto-fixable. `wikictl page upsert` auto-fixes before the write (markdown-fix → mermaid-validate → `PageUpsert`); the in-app editor warns non-blocking with the original text saved. SHA256 `e022302172162c294bcec1a0d3ee44938c765d15afb5b3e230fc48425d499f0e`. |
| [`plans/wikilink-fixer.md`](plans/wikilink-fixer.md) | **WikiLink Fixer (correct LLM-hallucinated bracket escapes).** `WikiLinkFixer` (renamed from `WikiLinkValidator`) strips trailing `\` from `[[link\]]` targets/aliases. Three surfaces: on-the-fly in `WikiLinkMarkdown`/`WikiLinkParser` (display), permanently in `MarkdownLinter.fix()` on every agent write, and explicitly via the "Lint" button / "Lint Page" context menu which pre-flights `WikiLinkFixer`, detects broken `[[page links]]`, then runs a page-scoped LLM lint with those findings baked into the prompt. |
| [`plans/sandbox-and-chat-modes.md`](plans/sandbox-and-chat-modes.md) | **Planned.** Re-add the sandbox toggle to Settings (governs the main agent + Edit chat; `fb8b20d` removed the UI), make `~/.claude/` + `~/.claude.json` writable in both seatbelt profiles (so a sandboxed session can write its transcript without EPERM), and replace the single Query chat with two **independent persistent sessions** — **Ask** (read-only, always sandboxed) and **Edit** (can write) — each its own button and tab, both open at once, no mode-switch restart. Serialized (one generates at a time); true concurrency deferred. |
| [`plans/sandbox-always-on.md`](plans/sandbox-always-on.md) | **Implemented (`security/sandbox-ingest-edit-by-default`).** The sandbox is **not configurable** — there is no `sandbox-config.json`, no `enabled` toggle, no `extraAllowedPaths`. Confinement is fixed by spawn type: Ingest/Edit get the write whitelist, Ask gets the read-only profile, both always on. Deleted `SandboxConfig.swift` + its tests and the `extraAllowedPaths` plumbing from `SandboxProfile`. Supersedes the opt-in "Config" section of `sandbox-agent.md`. |
| [`plans/acp-backend-and-permissions.md`](plans/acp-backend-and-permissions.md) | **Design + spike (delivers #286/#287, first slice of #217).** always-ask/yolo via the Agent Client Protocol: the app is the ACP client, and `session/request_permission` is the **structural** approval gate (the agent blocks on the tool call until the app decides) — no prompt-only enforcement, no MCP. yolo = auto-allow; always-ask = surface an Approve/Reject in the chat and block until decided (mirrors `~/work/paseo`'s `ACPAgentSession.requestPermission`). New `ACPBackend: AgentBackend` conformer (the port anticipates it); agent subprocess pluggable via the agent-command config (Claude via `@agentclientprotocol/claude-agent-acp`, or any ACP agent). Swift SDK candidate `wiedymi/swift-acp`; fallback hand-rolled JSON-RPC. Caveat: enforcement depends on the agent emitting `request_permission`. |
| [`plans/hover-wikilink.md`](plans/hover-wikilink.md) | Wiki links in `WikiReaderView` now show human-readable `title` tooltips on hover — `[[Page]]`, `[[source:Paper#"quote"]]`, and same-page anchor links reconstruct their original `[[…]]` notation; external links show the URL. |
| [`plans/bookmarks-tree.md`](plans/bookmarks-tree.md) | **Bookmarks sidebar section** — a fourth sidebar tab with a user-defined tree of folders, page/source references. Schema v16/v17 (`bookmark_nodes` table), `BookmarkTreeBuilder` pure-logic tree assembly, `BookmarksContainerView`/`BookmarksOutlineView` UI, `EditBookmarkSheet`/`ItemPickerSheet`/`BookmarkTargetPickerSheet` (item-first entry point — see `multi-select-bookmark.md`). Store CRUD + cycle prevention tests, tree-builder tests. |
| [`plans/graph-model-and-versioning.md`](plans/graph-model-and-versioning.md) | **Design of record for storage evolution; Phase 0 + Phase 1 + Phase 2 (tracks A+B) + Phase 3a + Phase 4 foundation implemented.** Successor to the `source-versioning-and-providers.md` draft. Git's objects-vs-refs discipline in SQLite: content-addressed `blobs`, append-only `source_versions` + `provider_runs`, a mutable `refs` table for active-version pointers, role-typed reference edges (`cite`/`embed`/`render`, `![[…]]` embeds), extraction alternatives, ULID-canonical link targets (save-time normalization, display-at-render), `@vN` version pinning, and the Phase 0 concurrency substrate (method-atomic store, savepoint transactions, `WikiReadPool`). Records the "no CozoDB" decision. **Phase 1 (v20)** shipped: `sources.content` → immutable `blobs` + `source_versions` chain + `agents`/`activities` PROV substrate + `refs` pointer, ref-resolved `sourceContent`, store-level `appendContentVersion`/`rollbackSourceContent`, 11-field `changeToken`. **Phase 2 (v21)** shipped: CAS'd/provenance-carrying extraction alternatives on `source_markdown_versions` (`recordMarkdownExtraction`), `source-derived` ref + `setActiveMarkdown`, `revert` as a pointer copy, re-extract path, `wikictl source set-active`. Track C (full compare/nominate UI) is planned in [`plans/track-c-extraction-compare.md`](plans/track-c-extraction-compare.md). **Phase 3a** shipped: `SourceProvider` protocol + `MaterializedSource`/`SourceProvenance`/`SourceOrigin` value types, four providers (`LocalFileProvider`/`WebsiteProvider`/`ZoteroProvider`/`MarkdownFolderProvider`), real provider/URL provenance threaded through `addSource`/`appendContentVersion` (populating the previously-stubbed PROV columns), `sourceOrigin(sourceID:)` read, the four ingest entry points unified behind `storeMaterialized(_:)`, and `SourceDetailView` Origin row + `wikictl source info`. No schema change. See [`plans/phase-3a-providers-and-provenance.md`](plans/phase-3a-providers-and-provenance.md). **Phase 4 foundation (v22)** shipped: `sources.role` (`'primary'`/`'media'`) + `source_links` §4.4 rowid/role/pin rebuild + `source_links_edge` index + `SourceRole`/`isPrimary` seam + media filtering in the Sources UI. Embeds/render-dispatch/`original_path` deferred to the second Phase 4 handoff. **Phase 4a** shipped: `![[source:…]]` embed parsing + `role='embed'` edges + `BlobSchemeHandler` binary serving (`wiki-blob://`) + inline HTML rendering dispatched on MIME type. **Phase 4b** shipped: byteless external embeds — provider-player iframes (YouTube/Vimeo/Spotify/SoundCloud), direct-remote native `<audio>`/`<video>` (mp3/mp4/HLS), and the Apple Podcasts embed player, via one shared mechanism (`SourceEmbedDescriptor` + batched `embedDescriptors()` query + `ExternalEmbed` dispatch + target-first `embedHTML` ordering). No schema change. **Phase 4 close-out (website snapshots)** shipped: `WebsiteSnapshotExtractor` downloads page images off-main (caps: 20 MB/image, 30 images, 50 MB total), rewrites `<img src>` to relative `original_path` at the token level (D4 amends §7), `addSnapshotImage` stores per-snapshot `.media` sources (blob-only dedup), `siblingImageResolvers()` + `imageResolver` on `MarkdownHTMLRenderer.render` resolve relative srcs to `wiki-blob://source/<id>` at render. No schema change (`original_path` shipped v20). Remaining deferred: `json-render` generative-UI spec, `apple-ttml` transcript-level PROV, snapshot-aware refresh. **Phase 5 (v23)** shipped: ULID-canonical link targets — `WikiLinkRewriter.canonicalize` normalizes resolvable `[[…]]` to `[[page:ULID\|alias]]`/`[[source:ULID\|alias]]` at the shared `PageUpsert` seam; display-at-render resolves ULID→current name (stale alias self-heals); `?id=` URL contract + id-based click routing; v22→23 data-only body sweep; `renameSource`'s body-rewrite loop deleted (rename = one-row metadata update). See [`plans/phase-5-link-canonicalization.md`](plans/phase-5-link-canonicalization.md). **Phase 6** shipped: `@vN` version pinning — `[[source:X@v3#"quote"]]` pins the Nth extraction (oldest = `v1`) so a quote highlight survives re-extraction. Pin-aware parse/canonicalize (preserves `@vN` verbatim), `replaceLinks` writes `pinned_version_id`, render-time `&pin=<smvID>` for quote links, and a pinned-extraction viewer in `SourceDetailView`. No schema change (`pinned_version_id` shipped v22). See [`plans/phase-6-pinning.md`](plans/phase-6-pinning.md). |
| [`plans/track-c-extraction-compare.md`](plans/track-c-extraction-compare.md) | **Planned (Phase 2 track C).** The compare/nominate UI deferred from the graph-model Phase 2 gate: a modal "Compare Extractions" sheet on `SourceDetailView` that renders any two extraction alternatives side-by-side in the real reader (`WikiReaderView`), shows each one's provenance (backend, model, date, size), and nominates the active one ("Set Active") via the existing `source-derived` ref. Reuses tracks A+B storage verbatim — no schema/migration change. A new `processedMarkdownAlternatives` query + `ExtractionAlternative` value type consolidate the history+agent-names calls and resolve backend display names. Raw text diff deferred to a fast-follow. |
| [`plans/address-bar-omnibox.md`](plans/address-bar-omnibox.md) | **Implemented.** Safari-style omnibox in the **window toolbar** (`.navigation` item, title removed): shows the active page's wikilink (`[[Title]]`) when idle, debounced semantic search (`store.searchSimilar`) when focused, ranked suggestions in a non-activating child `NSPanel`. AppKit `NSSearchField` (SwiftUI can't take first responder in `NSToolbar`), `OmniboxLayout` pure-math sizing engine (stretch/shrink/overflow-expand), keyboard nav in dropdown, Safari-style Page Menu (Zoom/Find) + add-bookmark chrome. Back/Forward + `WikiSwitcher` moved into the toolbar. No agent query in v1; no backend/schema changes. Tests: `OmniboxLayoutTests`. |
| [`plans/multi-select-bookmark.md`](plans/multi-select-bookmark.md) | **Implemented.** Bookmark from the Pages/Sources lists, not just from inside Bookmarks. Multi-select rows → **Add to Bookmarks…** context-menu action → `BookmarkTargetPickerSheet` (the inverse of `ItemPickerSheet`: items fixed, pick-or-create the destination folder). Inline "New folder" creation reuses `store.createFolder`; confirming creates one `pageRef`/`sourceRef` per selected item via `addPageRef`/`addSourceRef`. `BookmarkNode.displayPath(id:in:)` disambiguates same-named folders. Tests: `BookmarkNodeDisplayPathTests`. |
| [`plans/architecture-roadmap.md`](plans/architecture-roadmap.md) | **The map above the deep docs.** How the four large efforts relate and sequence — graph-model (storage) → #129 Resource abstraction (access) → partial #162 multi-window (client) → #187 gRPC daemon (distribution) — as three layers built bottom-up. ~~Records the one open sequencing decision (the #162 fork: thin slice now vs. full in-process refactor)~~ **§4 fork RESOLVED** — see `multi-wiki-daemon.md` (daemon-first). Also records the shared-file conflicts to sequence (`changeToken`, `Projection`, `WikiManager`). Read when adding a new large change or when the daemon moves from design to build. **#129 slice 2a (the event bus) shipped** — see `event-bus.md`. |
| [`plans/multi-wiki-daemon.md`](plans/multi-wiki-daemon.md) | **Phase 1 of #358: `wikid` XPC daemon + `WikiFSEngine` extraction.** Resolves the architecture-roadmap §4 fork with a third path — daemon-first, app-in-process, multi-window UI deferred to Phase 2. Extracts the agent execution engine (`AgentLauncher`, `ACPBackend`, `ClaudeCLIBackend`, `AgentOperationRunner`, `GenerationGate`, `ExtractionCoordinator`) out of the app target into a new `WikiFSEngine` library (both `wikid` and the app link it). The daemon owns the wiki registry + store lifecycle via XPC; `wikictl` is the first real client (registry ops + wiki resolution through the daemon, store writes still direct — "sole writer" deferred). **Phase 2a (the `WikiManager` dissolution) shipped** — see `plans/dissolve-wikimanager.md`. Supersedes the #358 design doc's "hybrid approach." |
| [`plans/module-restructure.md`](plans/module-restructure.md) | **Design (issue #532).** Research deliverable evaluating whether to split `WikiFSCore` (131 files, ~30k lines) into domain-specific SPM targets (`WikiFSLinks`, `WikiFSMarkdown`, `WikiFSSearch`, optional `WikiFSStorage`/`WikiFSModel`). Maps cross-domain coupling (SQLiteWikiStore + WikiStoreModel are fan-in hubs touching all clusters), runs an access-control audit (~22 types can become `internal`), measures build times (25s cached / 64s incremental), and recommends Option D phased — extract only the three low-risk pure-logic clusters, defer the storage/composition-root split. The one real cycle is `WikiStore.replaceLinks` ↔ `WikiLinkParser.ParsedLink` (fixable by moving the tiny struct to WikiFSCore). |
| [`plans/dissolve-wikimanager.md`](plans/dissolve-wikimanager.md) | **Phase 2a of #358: dissolve `WikiManager` into `WikiRegistryClient` + `WikiSession`.** Splits the monolithic app-scoped `WikiManager` into two focused types: `WikiRegistryClient` (app-scoped: wiki list + active id + create/select/delete/export/import) in `WikiFSCore`, and `WikiSession` (per-active-wiki: store + launchers + gate + vacuum) in `WikiFSEngine`. Each `WikiSession` creates its own `GenerationGate` + `AgentLauncher` pair, giving structural per-wiki isolation: a long ingest in one wiki cannot block a query in another. Multi-window (`WindowGroup(for:)`) is deferred to Phase 2b. |
| [`plans/multi-window-ui.md`](plans/multi-window-ui.md) | **Phase 2b of #358 (shipped).** Multi-window UI: `WindowGroup(for: String.self)` per-wiki-ID windows, a `SessionManager` (`@MainActor @Observable`) that owns the `[wikiID: WikiSession]` cache, `RootScene` as per-window entry point (session resolution, per-window scenePhase flush, vacuum alerts), `WikiChangeBridge.sessionLookup` closure (routes `wikictl`-write flushes to all matching sessions), `FileProviderSpike` multi-subscribe (one bus subscription per active session), `WikiSwitcher` `openWindow(value:)` + Option+click in-window switch, per-widget `VacuumCommands` via `SessionManager.frontmostSession`, and `ExtractionCompareContext` gaining a `wikiID` field. Tests: `SessionManagerTests` (9 tests), `WikiChangeBridgeTests` (4 tests). |
| [`plans/event-bus.md`](plans/event-bus.md) | **#129 slice 2a (shipped).** The per-wiki resource-change event bus: `WikiEventBus` + `ResourceChangeEvent` in `WikiFSCore`, store emission at the method-atomic `mutate()` seam (compute-while-locked, flush-after-unlock at depth-0), the File Provider signaler + the model's `.external`→reload path as subscribers on one mechanism, `WikiChangeBridge` as an `.external` adapter, and `onPageDidChange` fully deleted. Records the load-bearing invariant (every new public mutator must route through `mutate()` or be annotated no-emit), the `origin`/`seq` transitional fields, and the deferred 2b work (Resource protocol, model reload-on-self-write). |
| [`plans/resource-abstraction.md`](plans/resource-abstraction.md) | **#129 COMPLETE (slice 2a + 2b Phases A–D + Phase E shipped).** The Resource abstraction — a `Resource` leaf protocol + projection descriptors (`FlatResourceProjection`/`SingletonDoc`/`GeneratedIndex`/`NestedResourceProjection`) + a composed `changeToken` (per-kind fold contributors) + a per-wiki event bus with no `origin` distinction (Phase E: model subscribes to ALL events). Phase E follow-up: removing ~28 redundant per-call `reload*()` sites (code cleanup). See [`architecture-roadmap.md`](architecture-roadmap.md) §2.2/§3/§5/§6. |
| [`plans/page-versions-and-workspaces.md`](plans/page-versions-and-workspaces.md) | **Multi-writer concurrency.** Eliminates the global agent edit lock by implementing snapshot-isolation semantics over a durable workspace substrate. 5-phase plan: W0 (page versions + CAS conflict detection) → W1 (workspaces + fast-forward merge) → W2 (real diff3 merge) → W3 (conflict resolution UI) → W4 (concurrency at scale). **W0 shipped** (v30): `page_versions` table (append-only, blob-backed bodies), `refs` rebuilt with CHECK on `kind` (drops the `owner_id` FK so `page-content` refs work), CAS save protocol (`appendPageVersion` + `PageConflictError`), `wikictl page history` / `revert`. **W1 shipped** (v31): `workspaces` + `workspace_refs` tables, workspace CRUD (`createWorkspace`/`workspaceSummary`/`abandonWorkspace`), workspace writes (`workspaceWritePage` — append version + UPSERT overlay, doesn't touch main), overlay reads (`workspacePageVersion`), fast-forward merge (`workspaceMerge` — `main_head == base` → repoint + mirror; divergence → park `conflicted`), `wikictl workspace create/status/abandon/merge`. **W2 shipped**: real diff3 merge (`Diff3.swift` — line-based three-way merge with interleaved change detection), `workspaceMerge` now does diff3 instead of parking (merge version with `merge_parent_id` lineage + PROV `kind='merge'` activity + derived-data regen: `replaceLinks`/FTS/embeddings), `workspaceRefresh` verb (re-base workspace against current main without modifying main), `wikictl workspace refresh`. **W3 shipped** (v32): `workspace_conflicts` table (persists per-page conflict details on park), `workspaceConflicts` (query persisted conflicts), `workspaceResolveConflict` (write resolved body into workspace + clear conflict), `workspaceRetryMerge` (re-open + re-merge), `wikictl workspace conflicts/resolve/retry` verbs. **W4 shipped**: configurable N-throttle on `GenerationGate` (`maxConcurrent` parameter, default 1 for backward compat — when N > 1, up to N generations run simultaneously), workspace TTL reaper (`reapStaleWorkspaces(ttl:)` — abandons stale open workspaces from crashed runs), `wikictl workspace reap [--ttl <seconds>]` verb. See [`plans/w0-page-versions-cas.md`](plans/w0-page-versions-cas.md) through [`plans/w4-concurrency-at-scale.md`](plans/w4-concurrency-at-scale.md). |
| [`plans/usage-breakdown-by-model.md`](plans/usage-breakdown-by-model.md) | **Issue #583.** Per-model usage breakdown in the menu bar + Activity window. Adds `SessionUsage.modelName` (resolved from `ModelsInfo.availableModels` at the `ACPBackend` seam), `ModelUsageBreakdown` struct + `DailyUsageByModel` persisted to `UserDefaults` (daily-reset key), one disabled indented menu item per model below the "Today: 76K tokens · $1.23" summary line (heaviest first), and a per-item sub-view in the Activity window detail. Data flow unchanged — wired at the existing `.usage` queue-event handler in `QueueActivityTracker`. Future work: phase-level usage events, friendly-name lookup at the menu bar (`displayNameProvider` hook is ready), interactive chat usage (#546/#576). |
| [`plans/chat-and-persistence.md`](plans/chat-and-persistence.md) | **Chat UI + persistent chat (issue #119) — shipped.** Ask/Edit conversations persist to the wiki's SQLite store (v25 `chats` + `chat_messages`), render through a unified `ConversationView` with live streaming, and can be continued (seeded-fallback). The conversation layer is decoupled from the Claude-CLI wire protocol behind an `AgentBackend` port (`ClaudeCLIBackend` actor + per-turn `AsyncStream`). Covers: the backend port, `WikiRenderContext` (two-tier streaming render with healed links/embeds/pins), the source-of-truth rule (live vs persisted flip), takeover rules + preamble for continue, sidebar affordances (rename, live indicator, + New Conversation), the v25 schema bump, and what's deferred (Phase B resume, D5 profiles, v24 merge). Supersedes the deleted `persisted-chat-history.md`, `conversation-ui.md`, and `agent-backend-port.md`. |
| [`plans/chat-semantic-search.md`](plans/chat-semantic-search.md) | **Chat semantic search (issue #245) — shipped.** Past Ask/Edit conversations are now semantically + lexically searchable, mirroring the pages/sources pipeline: v28 `chat_chunks` (cosine embeddings) + `chat_search`/`chats_fts` (FTS5 sidecar), incremental write-time embedding of new user/assistant messages (append-only — never re-embeds prior turns), `searchSimilarChats` (hybrid RRF), a Chats sidebar search field, and `wikictl chat search`. |
| [`plans/podcast-transcripts.md`](plans/podcast-transcripts.md) | **Apple Podcasts transcript ingest.** Paste a `podcasts.apple.com` episode URL → the TTML transcript is fetched (via a private-framework FairPlay/Mescal signing helper, isolated in its own ObjC subprocess) and stored as a markdown source. Ported from PR #106 onto the Phase-3a `SourceProvider` protocol (Option A: transcript stored as source content); `ApplePodcastProvider` is the first real consumer of the protocol. Compiled out for App Store builds (`WIKIFS_APP_STORE=1`). The §11 byteless/derived-alternative remodel is deferred to Phase 4 (cheap: `recordMarkdownExtraction` + CAS make it a pointer move). |
| [`plans/source-format-materializers.md`](plans/source-format-materializers.md) | **Two-level dispatch: format layer + origin layer.** Extracts `FormatMaterializer.dispatch(data:contentType:stem:extensionHint:)` from `URLFetchService.plan(for:)` — a pure, URL-independent format dispatcher (HTML→Markdown, PDF/text/binary verbatim, content-sniffing). Every byte-producing origin (`WebsiteMaterializer`, `LocalFileMaterializer`, `ZoteroMaterializer`) routes through it. Fixes the Zotero bypass (HTML attachments now convert to Markdown instead of storing raw HTML). Prepares the seam for new origins (#261: git, Tavily, Slack). **Issue #599 update:** HTML sources are now treated like PDF sources — the original HTML bytes are the source blob (`SourceFormat.html`), and the extracted markdown rides as a `FormatPlan.extractedMarkdown` sidecar written as a `.extraction`-origin processed-markdown version (`appendExtractedMarkdown` in `WikiStoreModel`). `SourceDetailView` adds Reader/HTML/Split tabs for HTML sources, mirroring the PDF three-way. No migration — pre-#599 HTML sources stay markdown-only. |
| [`plans/chat-projection.md`](plans/chat-projection.md) | **Chat File Provider projection + `[[chat:…]]` wikilinks.** Chats (store v25) now project to the mount and are linkable from page/source bodies. Flat `chats/` tree (by-id + by-name), `chats.jsonl` index, `ResourceKind.chat` + token fold, store-mutator emission routing, `WikiLinkParser`/`WikiLinkMarkdown`/`WikiLinkRoute` `.chat`, canonical ULID form, `ChatTranscriptRenderer`, and the `wikictl chat list`/`get` agent surface. |
| [`plans/chat-quote-anchors.md`](plans/chat-quote-anchors.md) | **Chat quote anchors (issue #281).** `[[chat:Title#"quote"]]` deep-links to a specific message — navigates to the chat, scrolls to the message, and highlights the passage. Closes the chat-projection non-goal: a pure `ChatQuoteResolver` (whitespace-normalized first-match, mirroring the source quote anchor) + `WikiLinkRoute.chat` fragment plumbing + `ChatWebView` `window.find`/`<mark>` highlight reusing the reader's mechanism. |ject as a flat `chats/` tree (by-id + by-name), each rendered as a readable `.md` transcript via `ChatTranscriptRenderer`. Chat mutators route through `mutate()` and emit `.chat` events; a `ChatTokenContributor` (chat count + message count) extends the change token. `[[chat:Title]]` / `[[chat:<ULID>\|alias]]` wikilinks parse, render (`wiki://chat?title=…`), resolve (ghost vs live), navigate, and canonicalize — parallel to `[[source:…]]`. Includes `chats.jsonl` index, manifest count, `WIKI-STRUCTURE.md` chat entry. |
| [`plans/issue-235-ingest-extraction-lock.md`](plans/issue-235-ingest-extraction-lock.md) | **Issue #235 fix.** Prevents silent hang when starting Edit during an ingest's extraction phase: `isIngestInProgress` flag covers the extraction window + visible waiting caption replaces hidden tooltip. |
| [`plans/acp-stall-recovery.md`](plans/acp-stall-recovery.md) | **Issue #334 (Phase 1 = app-side, shippable alone).** Fixes the permanent ACP turn stall where `sendPrompt` never returns: a turn **inactivity watchdog** (`TurnLivenessPolicy` — idle 120s / ceiling 30 min, never a flat timeout), a **session-lifetime notification drain** (`NotificationFanout` — eliminates the per-turn AsyncStream re-acquisition race), and a Stop-path audit. Phase 2 (deferred): fork `wiedymi/swift-acp` for ordered transport reads, non-blocking incoming requests, stderr forwarding, PID exposure. Phase 3 (deferred): watchdog kill escalation + UI surfacing. |
| [`plans/queue-engine.md`](plans/queue-engine.md) | **Queue Engine.** A persistent extraction/ingestion work queue (`queue.sqlite`) with durable state transitions, crash recovery, and pruning. The `QueueEngine` actor dispatches items to workers with per-provider concurrency limits and a per-wiki ingestion invariant. All PDF extraction and ingestion flows through the engine; a menu-bar status item opens a unified Activity window showing all queue items with per-item agent transcripts. The app drops to accessory (menu-bar-only) mode when the last window closes — queue work keeps running. Lint is a payload variant of `.ingestion` (via `lintPageIDs`), sharing per-wiki serialization, capacity, and pause/resume/halt. |
| [`plans/issue-484-change-token-struct.md`](plans/issue-484-change-token-struct.md) | **Issue #484.** Replaces the positional colon-joined `changeToken()` string literals with a structured `ChangeToken` type — named fields per fold (`pages`, `sources`, `systemPrompt`, etc.) so adding a new `ResourceKind` fold no longer breaks ~20 tests that each need a `:0` appended. The contributor registry still drives construction; `ChangeTokenContributor.fragment(in:)` becomes `fold(in:)` returning a `ChangeTokenFold` enum case. A `rawString` property reproduces the colon-joined form for the File Provider sync anchor. |
| [`plans/acp-session-efficiency.md`](plans/acp-session-efficiency.md) | **Issue #525.** Design for optimizing the ACP session lifecycle in the multi-phase ingest pipeline. Replaces per-phase subprocess spawning (7 complete lifecycles for a 5-source ingest = 12–24s wasted) with a warm-subprocess model: one `launch` + `initialize` at ingest start, per-phase `session/new` + `session/close` (without `terminate`), and one `terminate` at the end. Four incremental phases: (1) warm subprocess + `closeSession` method; (2) `session/resume` crash recovery with death detection and session-ID persistence; (3) `session/fork` for executors to inherit planner context without reasoning noise; (4) parallel executors via `withTaskGroup` + context monitoring via `usage_update` notifications. Includes a context-benefit matrix (warm vs cold sessions), budget/cost tracking integration (`UsageUpdate.cost` → #528), and interaction analysis with the generation gate, `stopAgent`, and the watchdog. |
| [`plans/tantivy-search-sidecar.md`](plans/tantivy-search-sidecar.md) | **Issue #526.** Design research evaluating whether to replace FTS5 + sqlite-vec + RRF hybrid search with a Tantivy-backed search sidecar via [botisan-ai/tantivy.swift](https://github.com/botisan-ai/tantivy.swift). Covers build integration (XCFramework resolution, aarch64-only, binary size), document schema (one unified index with `kind` facet), sync architecture (event bus subscription → actor-isolated index writes, incremental upsert/delete, crash recovery rebuild from SQLite), search API replacement (unified `search(query:kinds:limit:)` with passage snippets, keep embeddings in SQLite + RRF fusion), fallback strategy (phase 1–2 keep FTS5 as fallback, phase 3 retire), performance benchmarks, and interaction with module restructuring (#532 WikiFSSearch) and GRDB (#530, complementary). Recommends a 4-phase plan: build spike → shadow index → cutover → retire FTS5+sqlite-vec. **Phases 0 + 1 + 2 shipped** — Phase 2 (feature/tantivy-search-cutover) wires Tantivy as the primary BM25 leg via an optional `bm25Leg` injection seam on `WikiStore.searchSimilar*` (Option B — proven in-store RRF untouched, semantic cosine unchanged; FTS5 stays as fallback when `bm25Leg` is `nil`/empty). The model resolves Tantivy hits → full summaries via cached catalogs and passes the leg through; `WikiSession.searchTantivy(query:kinds:limit:)` exposes the raw hits. Phase 3 (retire FTS5 + sqlite-vec) is next. |
| [`plans/tantivy-build-spike-results.md`](plans/tantivy-build-spike-results.md) | **Phase 0 build spike (branch `spike/tantivy-build-verification`).** Verifies the `botisan-ai/tantivy.swift` XCFramework resolves under bare `swift build` (no Xcode). Result: ✅ PASSED — builds in 68 s, zero warnings under `-warnings-as-errors`, smoke test (index + search round-trip) passes in 0.11 s. Key correction: the macOS slice is a **universal binary** (`arm64` + `x86_64`), contradicting the design doc's "Intel likely unsupported." Binary delta +12 MB (debug). Phase 1 (shadow index) is unblocked. |
| [`plans/grdb-adoption.md`](plans/grdb-adoption.md) | **Issue #530.** GRDB.swift adoption evaluation. Maps each hand-rolled SQLite feature to its GRDB equivalent (statement caching → automatic, `withTransaction` → `db.inTransaction`, positional columns → `Row.decode`, 37-version ladder → `DatabaseMigrator`, `WikiReadPool` → `DatabasePool`). Key finding: the `mutate()` seam is portable as a 15-line wrapper around `dbQueue.write { }`. `ValueObservation` complements (not replaces) `WikiEventBus` — the bus carries `kind`/`id`/`origin` metadata the File Provider needs. Custom extensions (sqlite-vec, zstd) work via `Database.sqliteConnection`. QueueStore pilot (#543) and full GRDBWikiStore (#545/#550/#557) shipped: all 88 methods implemented, 37-version migration ladder, zero stubs. Path to removing `SQLiteWikiStore`: parity tests → swap injection point → delete. |
| [`plans/module-restructure.md`](plans/module-restructure.md) | **Issue #532.** Module restructuring design (Option D — phased extraction). Extracted three pure-logic clusters from `WikiFSCore` into separate SPM targets: `WikiFSLinks` (11 files), `WikiFSMarkdown` (16 files), `WikiFSSearch` (7 files), plus a `WikiFSTypes` leaf (PageID, ULID, ResourceKind, EmbedTarget, ParsedLink, DebugLog). Uses `@_exported import` re-export pattern so consumers don't need hundreds of new imports. Key finding: `SQLiteWikiStore` is a fan-in hub (not a leaf) — it depends on all domain clusters but they don't call back. `WikiStoreModel` is the composition root (3,096 lines, 75+ store calls, 68 views observe it) — cannot be decomposed without refactoring 68 views. Defers `WikiFSStorage` extraction (high risk, uncertain payoff). |
| [`plans/provenance-panel.md`](plans/provenance-panel.md) | **Issue #745.** Provenance panel shows run names (not chat ULIDs) and makes entries clickable. Adds `runTitle` to `PageOrigin` (resolved via a `chats` table subquery), rewrites the panel's `agentLabel` to show chat titles / friendly run labels, and makes history rows clickable (chat → `openTab(.chat(id))`, agent run → Activity window). New `openActivityWindow` environment bridge from `OpenWindowBridge` → `MenuBarItemController` → SwiftUI environment. |
| [`plans/bun-prereq-check.md`](plans/bun-prereq-check.md) | **Issue #762.** Moves the bun prerequisite check up front so the build fails immediately (before `swift build`) when bun is missing. Two-part: an up-front gate in `build.sh` (before the compile) and a dedicated `bun-check` Makefile target wired into `build`/`release` only (not `check`/`test`). Same resolution chain (`BUN_INSTALL` → `~/.bun` → PATH) and error message as the original bundling logic. |
| [`plans/page-author-typing.md`](plans/page-author-typing.md) | **Issue #797 (Phase 1).** Typed `PageAuthor` + `AgentKind` enums in `WikiFSTypes` as the single source of truth for the `agents.name` / `agents.kind` string convention. Routes every construction and parse site (`AgentLauncher.authorForRun`, `GRDBWikiStore.authorKind`, `ProvenancePanel`) through them. Fixes three nil-author leaks that silently degraded chat/agent provenance to `legacy-import` (in-app rename → `user`, preflight lint auto-fix → `agent:lint`, daemon Home bootstrap → `user`). No DB migration. The four SQL `substr(a.name, 6)` strip sites stay raw SQL with a back-reference comment. Phase 2 (`SourceProvider` enum) + Phase 3 (`SourceContentType`) deferred. |

## Status

See `PROGRESS.md` for the running log. Current: **🎉 LLM Wiki COMPLETE ✅ — all
five phases (0, A, B, C, D) gate-passed.** Self Driving Wiki is now a self-maintaining LLM
wiki: a user keeps **many** wikis (one SQLite DB + one File Provider domain
each); an LLM (`claude -p`, run as **Ingest / Query / Lint** from the app)
authors and maintains each one — reading the read-only mount and writing via the
new **`wikictl`** CLI — keeping curated `index.md` + chronological `log.md`
current, cross-linking pages with clickable `[[wiki-links]]`, all under a real
maintainer schema projected as `CLAUDE.md`/`AGENTS.md`. Agent runs stream live
(tool calls + text, `--output-format stream-json`) with per-run backend
`run.jsonl` logs and an editor edit-lock. **All five phases plus the post-completion
features below are merged to `main` (single-branch repo, ready for developer
handoff). 341 tests green; clean signed bundle (app + appex + `wikictl`).**

**Graph-model Phase 5 (link canonicalization, v23) shipped.** Wiki-link targets
are now **ULID-canonical at rest** while authoring stays human-friendly: agents
and users keep writing `[[Some Title]]` / `[[source:Display Name]]`; at save
time each resolvable link is normalized to `[[page:ULID|Some Title]]` /
`[[source:ULID|Name]]`; at render time the display name is resolved from the
ULID so a stale alias self-heals. This kills the rename-silently-drops-link-rows
bug class. **Phase gate met: rename a page with 50 inbound links → zero bodies
rewritten, zero ghosts** (AC.1–AC.9, 1617 tests green). Unblocks Phase 6
(`@vN` pinning).

**Graph-model Phase 6 (`@vN` version pinning) shipped.** A wiki link can now pin
a specific derived-markdown extraction so that a **quote highlight survives
re-extraction**: `[[source:X@v3#"quote"]]` opens v3's content (not HEAD) so the
quote is present even after X is reprocessed. `@vN` is an ordinal into the
extraction chain (oldest = `v1`), stays in the body, and is resolved at save
(`pinned_version_id`) and render (`&pin=<smvID>` for quote links). No schema
change — code-only. **Phase gate met: 1653 tests green** (AC.1–AC.10). See
[`plans/phase-6-pinning.md`](plans/phase-6-pinning.md).

**Post-completion features (also on `main`):**
- **Reveal in Finder** — "Reveal in Finder" action on every page and source surface
  (sidebar context menu + detail view header). Calls
  `NSWorkspace.shared.activateFileViewerSelecting` after resolving the item's
  user-visible URL from the File Provider daemon (reuses `resolvePageByTitleURL` /
  `resolveSourceByNameURL`). Available only when `fileProvider.path != nil`; shown for
  single selection only (batch reveal deferred — would open N parallel Finder windows).
  Branch `feature/add-reveal-in-finder`, PR #90.
- **WikiLinkFixer + integrated page lint** — renamed `WikiLinkValidator` → `WikiLinkFixer` (it corrects, not merely validates). Added `WikiStoreModel.preflightLint` that applies `WikiLinkFixer.applyFixes()` and detects broken `[[page links]]` (targets with no matching wiki page). New `WikiOperation.lintPage` bakes those findings into the LLM prompt so the agent has concrete targets. "Lint" button (page detail toolbar) and "Lint Page" context menu (sidebar) now run both the regex pre-flight and the page-scoped LLM lint. The orange markdown-lint warning banner surfaces `\]]` issues; "Lint" is the cure.
- **Agent seatbelt sandbox (write whitelist)** — opt-in (Settings → Agent → Sandbox)
  confines the spawned agent's filesystem WRITES to a strict allowlist via macOS's
  seatbelt (`sandbox-exec`): only the per-run scratch dir + the active wiki's SQLite DB
  (+ `-wal`/`-shm`/`-journal`) are writable; reads/network/exec stay open. Provider-
  agnostic (wraps any `AgentCommandConfig` executable). The provider's config/temp are
  relocated into the scratch dir (`CLAUDE_CONFIG_DIR`, `TMPDIR`). Default-deny writes
  → no backdoors/credential-tampering, and cross-wiki DB confinement for free. Off by
  default (byte-identical runs until toggled). New `SandboxConfig` / `SandboxProfile`;
  `OperationCommand` gains a `sandbox:` param. 19 new tests (989 green). See
  `plans/sandbox-agent.md`.
- **File filter + batch ingest** — the Files section has a filter picker
  (All / Ready / Ingested) and a "Select…" button that enables batch mode with
  checkboxes. Selected files are ingested in a SINGLE agent run — all sources staged
  together so the agent cross-references and synthesizes holistically. The ingest
  pipeline was generalized from one source → N sources (`WikiOperation.ingest`,
  `AgentStaging.stageSources`, `AgentOperationRunner.runMultiIngest`).
- **Import Markdown Folder** — a one-shot "Import Markdown Folder…" action that
  recursively walks a directory of `.md` / `.markdown` files (Obsidian vault, LogSeq
  graph, or any folder) and lands them in `ingested_files` for the agent to curate
  via Ingest. Hidden files/dirs are skipped; duplicate filenames get a disambiguating
  suffix. `MarkdownFolderReader` (pure core) + `ImportMarkdownSheet` (phase-enum UI).
  26 new tests. See `plans/markdown-folder-import.md`.
  wiki, export its checkpointed standalone SQLite file, and import a SQLite wiki
  backup under a new display name/new ULID. Rename refreshes the File Provider
  display name while preserving identity; export refuses to overwrite its source.
- **Ingest model-tiering** — Ingest is now **Opus-curated**: Opus decides what goes
  in the wiki and writes every page; for a large source it fans out **2–19 Sonnet
  `source-reader` subagents** (via `claude -p --agents`) that only *digest* the
  bulk content and return extracts (they never write), and Opus may fork follow-up
  readers / pull pages to double-check. Tiny sources are a single Opus pass. The
  per-run scratch dir stages the source + a live `WIKI_STATE.md` snapshot from
  SQLite so the agent never re-derives structure from the laggy mount.
- **Ingest a resource by URL** — an "Add from URL…" sheet fetches a URL, normalizes
  known file-share links (Dropbox `www`→`dl`; Drive/OneDrive stubbed), content-sniffs
  the bytes, converts HTML→Markdown (hand-rolled, dependency-free) or stores
  PDFs/binaries verbatim — landing through the same ingest path as drag-drop.
- **PDF extraction pipeline** — local `pdf2md` script (docling + granite-docling VLM)
  converts PDFs to Markdown before the agent sees them. `PdfExtractionService` spawns
  `pdf2md` as a subprocess with continuous pipe draining; `PdfExtractionView` shows
  readiness, download progress, and live conversion log during ingest. 22 Swift tests
  + 67 Python tests. (PR #11.)
- **Serialized claude spawn slot** — the single shared `AgentLauncher` now serializes
  all `claude -p` spawns (ingest / query / lint) through a FIFO, cancellation-aware
  spawn slot (`awaitSpawnSlot` / `releaseSpawnSlot`). Two claude runs never overlap;
  a `pdf2md` extraction may overlap a claude query run (it does NOT take the slot).
  The old silent-drop `guard !isRunning` is gone — a query started during an ingest's
  extraction now runs, and the ingest agent waits for the slot and runs afterward. The
  edit lock (`store.isAgentRunning`) is unchanged in meaning: locked only while a
  claude process runs, unlocked during extraction. The Query page mounts the orange
  `AgentRunBanner` and scopes its debug cluster to active query runs only. 6 new
  `AgentSpawnSlotTests`.

**Phase summary (newest first; see `PROGRESS.md` for each gate's evidence):**
- **Page body contract** ✅ `body_markdown` in SQLite now stores clean body only
  (no H1, no frontmatter); `PageMarkdownFormat` strips both on load/rename and
  regenerates them in the file provider (`Projection`); outline flicker
  eliminated; editor warns immediately when the user types frontmatter.
  See `plans/page-body-contract.md`.
- **Phase D — the schema** ✅ real maintainer `CLAUDE.md` schema (layout,
  conventions, `wikictl` reference, read-after-write rule, Ingest/Query/Lint
  playbooks); `-p` prompts slimmed to rely on it; new wikis seed it, existing
  unaffected. Also hardened File Provider domain registration (verify/retry/nudge/
  surface-errors).
- **Phase C — `claude -p` operations** ✅ Ingest/Query/Lint scoped runs +
  `--dangerously-skip-permissions` + layout-up-front (`TREE.md`) + live streaming
  panel + backend logs + per-wiki edit-lock + clickable `[[wiki-links]]`.
- **Phase B — `log.md` + `index.md`** ✅ v3→4 `log` table + v4→5 `wiki_index`
  singleton; `wikictl log append` / `index set`; both projected read-only at root;
  `changeToken()` folds.
- **Phase A — write path + change bridge** ✅ `wikictl` (page upsert/get/list/
  delete) + shared `PageUpsert` link-reparse + per-wiki Darwin notification →
  debounced sidebar rebuild + `signalChange()`.
- **Phase 0 — many wikis** ✅ wiki registry (ULID identity), per-wiki DBs +
  per-wiki File Provider domains, in-app switcher, v0 wiki migrated as wiki #1.

**Prior: LLM Wiki Phase A (Write path + change bridge) DONE ✅ — live gate
passed.** The `wikictl` CLI (`page list/get/upsert/delete`, selecting a wiki via
`--wiki`/`WIKI_DB`) writes straight to a wiki's `<ulid>.sqlite`; a shared
`PageUpsert` op keeps the `[[link]]` graph identical across the app and the CLI;
`wikictl` posts a per-wiki Darwin notification and the app's debounced change
bridge rebuilds the sidebar + `signalChange()`s that wiki's mount. 113 tests;
clean signed bundle (app + appex + `wikictl`). Branch `llmwiki/phase-a-write-path`
(stacked on `llmwiki/phase-0-many-wikis`, unmerged).

**Prior: LLM Wiki Phase 0 (Many wikis) DONE ✅ — live gate passed.** One SQLite DB
+ one File Provider domain **per wiki**, a `wikis.json` registry, an in-app
create/select/delete switcher, and the single v0 wiki migrated in as wiki #1
(idempotently). Branch `llmwiki/phase-0-many-wikis`. See `plans/llm-wiki.md` for
the Phase 0 → A–D plan.

**Prior baseline: 🎉 v0 DONE ✅ — all four phases gate-passed (M0–M6).** A native macOS SwiftUI wiki, SQLite-backed,
projected read-only onto the filesystem via a File Provider extension, kept
fresh on edit, and traversable by an agent launched with `WIKI_ROOT`. Delivered
across four stacked, **unmerged** branches off a pristine `main`
(`phase-1-local-wiki` → `phase-2-file-provider` → `phase-3-verify-fresh` →
`phase-4-agent-wiki`) — review and merge locally. See `PROGRESS.md` for each
gate's evidence and the known v0 gaps.

**Post-v0 features** (also stacked, unmerged):

- `phase-5-file-ingest` — drag a file in to **ingest** it (raw bytes + metadata
  stored in a new `ingested_files` SQLite table, NOT a wiki page; surfaced
  read-only under `files/by-id` & `files/by-name`; removable "Files" list).
  Verified with a real 8 MB PDF served byte-identical from the mount.
- **System-prompt document** — a user-editable singleton "system prompt" (DB
  `system_prompt` table, v2→3 migration) projected **read-only at the wiki root
  as both `CLAUDE.md` and `AGENTS.md`** (identical bytes). Edited in-app via a
  pinned sidebar item. Code complete + unit-tested (69 tests); **live-mount gate
  pending**. See `PROGRESS.md`.

## Milestones (from `plans/INITIAL.md`)

- **M0 — App skeleton** ✅ build environment + launching SwiftUI window.
- **M1 — Markdown editor** ✅ sidebar page list, `TextEditor`, preview, autosave, SQLite persistence.
- **M2 — File Provider domain** ✅ extension target, domain registration, static root + `README.md`.
- **M3 — SQLite-backed page files** ✅ `pages/by-id`, `pages/by-title`, content from SQLite.
- **M4 — Path button** ✅ `Copy Unix Path`, verification commands in-app.
- **M5 — Change signaling** ✅ edits increment version; Terminal reads see updates (no relaunch).
- **M6 — Agent launch** ✅ spawn agent with `WIKI_ROOT` env pointing at the projection.

### Target-linking constraint (File Provider extension)

The `WikiFSFileProvider` extension is `com.apple.fileprovider-nonui` and
**must not link AppKit** (or PDFKit/AVFoundation/Metal/Accelerate/CoreML) —
macOS 26 asserts against it and the extension crashes on launch in
`_EXRunningExtension._start`. The extension links `WikiFSCore` read-only as its
sole dependency, so **`WikiFSCore` itself must stay free of those frameworks**.
Anything framework-backed that `WikiFSCore` needs (currently PDF-title extraction
via PDFKit) is exposed behind an injectable seam (`DisplayNameResolver.pdfTitle-
Extractor`) whose default is framework-free; the real implementation lives in the
**app** target (`Sources/WikiFS/`), which the extension does not link, and is
installed at launch. Verified by `otool -L` on the built extension binary.
See `plans/file-provider.md`.

## Build quick reference

```sh
make          # debug build → build/Self Driving Wiki.app
make run      # build + launch
make check    # compile-only gate (no bundle/sign)
make help     # all targets
```

Full detail: [`plans/build-environment.md`](plans/build-environment.md).

### Versioning

Version is derived from git at build time. Two codegen paths keep code and
bundle in sync:

1. **`tools/versiongen/main.swift`** → `Sources/WikiFSCore/GeneratedVersion.swift`
   — a gitignored, build-time-generated Swift file with `appVersion`, `gitSHA`,
   `gitCommitCount`, `buildVersion`, `fullVersionString`. Regenerated by `make
   version` (a prerequisite of `build`/`check`/`test`/`release`); bare `swift
   build` does NOT regenerate it — run `make version` first (CI runs `make
   version prompts` before `swift build`). Never committed to git (git is
   already the source of truth, so a committed snapshot would drift on every
   commit).

2. **`build.sh`** — resolves `VERSION` (SemVer from `vX.Y.Z` tag or `VERSION`
   file, `0.0.0` fallback) + `BUILD_VERSION` (`<commit-count>-<short-sha>`)
   and writes them into both Info.plists:
   - `CFBundleShortVersionString` = marketing version (e.g. `0.5.0`)
   - `CFBundleVersion` = build identifier (e.g. `487-abc1234`)
   - `WIKIGitSHA`, `WIKIGitCommitCount`, `WIKIBuildVersion` — custom keys for
     runtime query.

`wikictl version` / `--version` / `-v` prints build info (no wiki required).
`make print-version` shows the resolved version from the Makefile level.

The app's Settings → About tab (first tab) displays version, build, and SHA.
`ACPBackend` sends `GeneratedVersion.appVersion` as the ACP client version
(was hardcoded `"1.0.0"`).
